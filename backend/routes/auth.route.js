const express  = require('express');
const router = express.Router();

router.get('/me',); //profile page
router.post('/signup',); //signup page
router.post('/login',); //login page
router.post('/logout',); //logout page

module.exports = router;
